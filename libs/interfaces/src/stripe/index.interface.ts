/**
 * @description A Checkout Session controls what your customer sees in the Stripe-hosted
 * payment page such as line items, the order amount and currency, and acceptable payment methods.
 */
export interface IStripeCheckoutSession {
  _id: string;
}
